# Pixeltable - Multimodal AI Data Infrastructure
pixeltable>=0.5.0

# FastAPI for HTTP endpoints
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# OCR Engines (from OCR_scripts)
easyocr>=1.7.0
paddleocr>=2.7.0
paddlepaddle>=2.5.0

# Roboflow for detection
roboflow>=1.1.0

# Roboflow Serverless inference (SmolVLM2)
inference-sdk>=0.20.0

# Image processing
opencv-python>=4.8.0
pillow>=10.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Utils
python-dotenv>=1.0.0
pydantic>=2.0.0

# AWS S3
boto3>=1.34.0

# Super-Resolution (ISR library)
# NOTE: ISR 2.2.0 requires tensorflow==2.0.0, which is no longer available.
# We install a compatible TensorFlow version manually.
# ISR will be installed via pip install --no-deps in the Dockerfile.
tensorflow>=2.12.0,<2.16.0
imageio>=2.9.0
h5py>=2.10.0
